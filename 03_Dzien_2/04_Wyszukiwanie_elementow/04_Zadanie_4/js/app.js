const navList = document.querySelectorAll('nav.navigation li');
console.log(navList.length);


const allDiv = document.querySelectorAll('div p');
console.log(allDiv.length);

const articleDivs = document.querySelectorAll('article div');
console.log(articleDivs.length)
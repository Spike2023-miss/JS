const timeMachine = {
    shape: "square",
    model: "sl500",
    run(date, place) {
        console.log(`jest rok ${data} i jest w ${place}`)
    }
}
console.log(timeMachine.shape)
console.log(timeMachine.model)

timeMachine.run(2004, "kraków")